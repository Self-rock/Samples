



//для установки локали для терминала, перед cin cout применить
    //system("chcp 65001");//для utf8 , или другую локаль, в которой сохранены исходники
    //system("chcp 65001 > nul"); то же самое, чтобы консоль не выводила сообщение "Active code page: 65001"
    //system("chcp 1251"); ТОЖЕ ЧТО И ВЫШЕ

    //system("chcp 866");
    


    /*
    system("color 0A") - Меняет цвет фона консоли, зеленый текст на черном фоне
    system("color a") зеленый текст на черном фоне
    
    
    system("cls") - Очищает консоль
    
    
    system("pause") //пауза для ожидания нажатия клавиш


    system("print '.\текущий PATH.txt'") печать файла

     system("echo "добавляем текст" >> del.txt");//добавление текста в конец файла
    */


    /*очистка экрана
    void clearScreen(){
    #ifdef _WIN32
        system("cls");
    #else
        system("clear");
    
    #endif 
   }
    */
   